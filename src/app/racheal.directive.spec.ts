import { RachealDirective } from './racheal.directive';

describe('RachealDirective', () => {
  it('should create an instance', () => {
    const directive = new RachealDirective();
    expect(directive).toBeTruthy();
  });
});
